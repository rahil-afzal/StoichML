# Ignore all Python cache directories anywhere
__pycache__/
*.py[cod]

# Data artifacts (regenerable via scripts/run_featurize.py)
data/data_feat.pkl
egap_comparison
# Jupyter
.ipynb_checkpoints/
data/test.ipynb

# Model outputs (regenerable via scripts/model_*.py)
# NOTE: this deliberately no longer contains a blanket "*.json" rule.
# That rule also matched data/selected_features.json and the per-task
# metrics JSONs in results/ — both are small, tracked result artifacts,
# not generated scratch output, and should not be gitignored.
models/
shap_outputs/
topk/
